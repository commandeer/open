myFileEvent:
  handler: myFileEvent.handleSftpFile
  timeout: 900

  events:
    - existingS3:
        bucket: ${self:custom.bucket-name}
        event: s3:ObjectCreated:*
        rules:
          - suffix: .csv
